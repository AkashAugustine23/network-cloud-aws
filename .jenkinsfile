
pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                script {
                    // Checkout the Git repository
                    checkout scmGit(
                        branches: [[name: '*/master']], 
                        extensions: [], 
                        userRemoteConfigs: [[url: 'https://github.com/AkashAugustine23/network-cloud-aws.git']]
                    )
                }
            }
        }
        
        // Other stages for your pipeline go here...
    }
}
